# Needs you

- 🟢 **[simple] Confirm the last Pages run is green** (Actions tab). The final
  volume-control push (end of 2026-06-11 evening session) couldn't be watched locally —
  api.github.com was timing out on this machine. v2 itself is already confirmed live;
  if that last run failed for some environment reason, re-run it from the Actions tab.

- 🟢 **[simple] Repo polish (optional):** add repo topics (`phaser`, `typescript`,
  `bullet-heaven`, `game`) and a social-preview image on GitHub — cosmetic, needs repo admin UI.

- ~~🟡 Push `main` to deploy~~ — resolved 2026-06-11: Rodrigo authorized in-session;
  v2 deployed and content-verified live.
- ~~🟢 Decide gold's future~~ — resolved 2026-06-11: gold is a spendable currency
  (Crypt Shop), banked across runs.
