# Lightweight proxy runtime only. We intentionally avoid LiteLLM's full
# `proxy` extra because this project already owns the developer dashboard and
# does not need its analytics, enterprise or cloud-storage dependencies.
litellm==1.96.2
fastapi==0.140.0
uvicorn==0.52.3
pyyaml==6.0.3
python-multipart==0.0.32
PyJWT==2.13.0
backoff==2.2.1
websockets==15.0.1
orjson==3.11.9
APScheduler==3.11.3
redis==8.1.0
cryptography==50.0.0
boto3==1.43.71
expression==5.6.0
email-validator==2.3.0
fastapi-sso==0.21.1
uvloop==0.22.1
